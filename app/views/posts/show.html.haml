%h1 投稿詳細ページ
%h3= @post.user.email
%h3= @post.content
-# %h2 コメント一覧
-# - @comments.each do |c|
-#   %div
-#     %a{:href => "/users/#{@post.user.id}"}= c.user.email
-#     = c.content
-#     %hr/
-# = form_for [@post, @comment] do |f|
-#   = f.text_field :content
-#   %br/
-#   = f.submit 'コメントする'
= link_to "ホームへ戻る", posts_path
以上です

